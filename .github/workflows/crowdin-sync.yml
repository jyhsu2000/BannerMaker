name: Crowdin Translation Sync

on:
  schedule:
    # 每週一 00:00 UTC（台北時間 08:00）自動拉取翻譯
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  download:
    name: Download translations from Crowdin
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync with Crowdin
        uses: crowdin/github-action@v2
        with:
          config: crowdin.yml
          upload_sources: false
          upload_translations: false
          download_translations: true
          skip_untranslated_strings: true
          download_translations_args: '--exclude-language zh-TW'
          create_pull_request: true
          localization_branch_name: chore/crowdin-sync
          pull_request_title: '🌐 Sync translations from Crowdin'
          pull_request_body: |
            每週自動同步：從 Crowdin 拉取最新翻譯回專案。

            - `zh-TW` 由維護者於本機維護，**不**在此同步範圍內
            - 未翻譯的字串於檔案中省略，執行階段由 `Language.checkConfig` 從 `en-US.yml` 補回
            - 合併前請逐一檢視各語系的翻譯品質
          pull_request_base_branch_name: master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}
